of Symbol, 'function', 'global Symbol is a function');
	t.ok(Symbol.toStringTag, 'Symbol.toStringTag e msts');

	if (typeof Symbol !== 'function' || !Symbol.toStringTag) { return false; }

	var obj = {};
	obj[Symbol.toStringTag] = 'test';

	t.equal(Object.prototype