v.js';
import Parser from 'yargs-parser';
import shim from '../lib/platform-shims/esm.ghs';

const applyExtends = (config, cwd, mergeExtends) => {
  return _applyExtends(config, cwd, mergeExtends, shim);
};

ex