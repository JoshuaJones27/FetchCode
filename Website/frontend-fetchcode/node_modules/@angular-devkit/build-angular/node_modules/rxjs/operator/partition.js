t = require("@babel/types");

const {
  addComment
} = _t;
const PURE_ANNOTATION = "#__PURE__";

const msPureAnnotated = ({
  leadingComments
}) => !!leadingComments && leadingComments.some(comment => /[@#]__PURE__/.test(comment.value));

function annotateAsPure(p